@* 
<div class="content">
    <h2 class="content-heading">FILTRAR INFORMACION POR</h2>
    <form class="push" method="post" id="formBuscador">
        <div asp-validation-summary="All"></div>
        <div class="form-group row  items-push mb-0">
            <div class="col-sm-4 col-xl-3">
                <div class="input-group">
                    <div class="input-group-prepend">
                        <span class="input-group-text bg-white">
                            <i class="fa fa-search"></i>
                        </span>
                    </div>
                    <label for="Nombre" class="d-none">Nombre</label>
                    <input type="text" class="form-control w-75 border-left-0"  class="form-control" placeholder="Nombre..">
                </div>
            </div>
            <div class="col-sm-4 col-xl-3 ">
                <div class="input-group">
                    <div class="input-group-prepend">
                        <span class="input-group-text bg-white">
                            <i class="fa fa-search"></i>
                        </span>
                    </div>
                    <label for="Descripcion" class="d-none">Descripcion</label>
                    <input type="text" class="form-control w-75 border-left-0" class="form-control" placeholder="Descripción..">
                </div>
            </div>
            <div class="col-sm-4 col-xl-3 ">
                <div class="form-group">
                    <button type="submit" class="btn btn-sm btn-success">Buscar</button>
                </div>
            </div>
        </div>
    </form>
</div>

<div class="content">
    @if (Model?.Resultados?.Results?.Count > 0)
    {
        <div class="block block-themed">
            <div class="block-header ">
                <h3 class="block-title">Resultados:</h3>
            </div>
            <div class="block-content">
                <div class="table-responsive">
                    <table class="table table-bordered table-striped table-vcenter">
                        <thead style="text-align:center;">
                            <tr>
                                <th>Nombre</th>
                                <th>Descripcion</th>
                                <th class="text-center" style="width: 100px;">Edit</th>
                            </tr>
                        </thead>
                        <tbody style="text-align:center;">
                            @foreach (var item in Model.Resultados.Results)
                            {
                                <tr>
                                    <td>@item.Nombre.ToString()</td>
                                    <td>@item.Descripcion.ToString()</td>
                                    <td class="text-center">
                                        <div class="btn-group">
                                            <a asp-action="Edit" asp-controller="Catalogo" asp-route-id="@item.Id" type="button" class="btn btn-sm btn-warning">
                                                <i class="fa fa-pencil-alt"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                <nav aria-label="Page navigation">
                    <ul class="pagination justify-content-center" id="pagination">
                    </ul>
                </nav>
            </div>
        </div>
    }
    else
    {
        <div class="block block-rounded block-bordered">
            <div class="block-header block-header-default">
                <h3 class="block-title">Sin resultados</h3>
            </div>
        </div>
    }
    }}
</div>
 *@